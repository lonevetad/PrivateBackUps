package common;

import common.abstractCommon.AnimationName;

public enum Animations implements AnimationName {
	Idle, Walking, Running, Dying, Hitted, ActionGeneric, Opening, Firing, SpecialAbility, Upgrading, Highlighted;
}